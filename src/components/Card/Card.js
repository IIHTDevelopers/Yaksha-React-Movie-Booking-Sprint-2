import React from 'react'
import styles from './Card.module.css'

const Card = ({ movie }) => {
  const bookShow = () => {
    
  }
  return (
    <div >
    </div>
  )
}

Card.propTypes = {}

Card.defaultProps = {}

export default Card
